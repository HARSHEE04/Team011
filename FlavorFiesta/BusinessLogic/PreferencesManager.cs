using System;
namespace FlavorFiesta.BusinessLogic
{
	public class PreferencesManager
	{
		//define a list


		// keeps a track of all previous preferences, each time user chooses
		// preferences, we add it to the list of preferences so user has access 
        public PreferencesManager()
		{
		
		}
		private void AddPrefernces()
		{
			//return it to the prefernces class as a list
			// add to the list
			return; 
		}

		private void RetriveUserInfo()
		{
			//takes the user information, and takes them into a listview page where
			//they can either do previous prefernces or new prefernces
			return;
		}
	}
}

